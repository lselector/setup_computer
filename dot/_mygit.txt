# .mygit

# note - Git also automatically reads the ~/.gitconfig file.

# --------------------------------------------------------------
if [ -f ~/.git-completion.bash ]; then
    . ~/.git-completion.bash
fi

# --------------------------------------------------------------
# gist, gitup, qq, gg were moved from here 
# into small bash scripts in "bin" directory in path

